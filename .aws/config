# Project-local AWS config for my-translator-pwa.
# Point the CLI/SDK at this directory instead of ~/.aws:
#   AWS_CONFIG_FILE=.aws/config
#   AWS_SHARED_CREDENTIALS_FILE=.aws/credentials
#   AWS_PROFILE=my-translator-pwa

[profile my-translator-pwa]
region = ap-southeast-1
output = json
